const accountId = 144553
let accountEmail = "badarsh2001@gmail.com"
var accountPassword = ""
accountCity = "Hyderabad"
let accountState ;

console.table([accountId, accountEmail, accountPassword, accountCity, accountState]);

// we can also use console.log but table is little better to print multiple values at the same time

/*
Use only let
var is a function scoped
let is block scoped
var is mostly hoisting based little advance for now
*/
