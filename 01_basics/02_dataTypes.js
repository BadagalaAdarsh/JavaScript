"use strict"; // it means all the JS code as newer version of the JS

// alert("hello") // since we are using node js and not browser based node so its not gonna work

// use mdn and tc39 documentation for the reference

let name = "adarsh";
let age = 18;
let isLoggedIn = false;


// primitive data types
// number is having a range somewhere around 2^53
// bigint
// string we can use either single or double quotes
// boolean 
// null ==> its a standalone value also a type in js (null is representation of empty, we can assign null to a variable)
// undefined ==> 
// symbol ==> unique 

// object

console.log(typeof ); // type of null is object and type of undefined is undefined